import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bongohorse.monstergirldelivery.perf330before',
  appName: 'MGD 330 BEFORE',
  webDir: 'dist',
  android: {
    appendUserAgent: 'MGD-Capacitor/1 MGD-Perf330-BEFORE/1',
    backgroundColor: '#121426',
  },
};

export default config;
